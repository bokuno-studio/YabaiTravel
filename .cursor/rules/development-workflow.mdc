---
description: チケット駆動開発ワークフロー - 自律的にチケット作成・レビュー依頼・実装
alwaysApply: true
---

# チケット駆動開発ワークフロー

ユーザーが「進めて」「続けて」「プロジェクトを進めて」等と言った場合、以下の順で実行する。

## 1. 承認済みチケットの確認

```bash
gh issue list --label approved --state open
```

- **結果あり** → 2へ
- **結果なし** → 3へ

## 2. 実装（承認済みチケットがある場合）

1. 該当 Issue に `in-progress` ラベルを追加
2. チケット内容に従って実装
3. 完了後、`done` ラベルを付与してクローズ
4. ユーザーに完了報告

## 3. チケット作成（承認済みがない場合）

1. `docs/OUTLINE.md` および `docs/WORKFLOW.md`、`docs/` 内の設計を参照
2. 次の優先タスクを特定し、GitHub Issue を作成
3. ラベル `pending-review` を付与
4. ユーザーに「以下のチケットをレビューしてください」と伝え、Issue 番号と概要を提示

```bash
gh issue create --title "タイトル" --body "本文" --label "pending-review"
```

## 4. ラベルが未作成の場合

初回は以下でラベルを作成する:

```bash
gh label create "pending-review" --color "FBCA04" --description "レビュー待ち"
gh label create "approved" --color "0E8A16" --description "承認済み"
gh label create "in-progress" --color "1D76DB" --description "実装中"
gh label create "done" --color "EDEDED" --description "完了"
```

## 5. ユーザーが「#123 承認」等と言った場合

該当 Issue に `approved` ラベルを追加し、実装に着手する。

## 6. 変更後のGit運用とデプロイ監視

ファイルを編集したら、**必ずコミットしてGitHubにプッシュ**する。

```bash
git add -A && git status
git commit -m "適切なコミットメッセージ"
git push
```

**プッシュ後は必ずデプロイを監視する:**

```bash
npm run deploy:watch
```

- staging にプッシュ → `staging-deploy-vercel.yml` を監視（ビルド＋Vercel デプロイ）
- main にプッシュ → `deploy-vercel.yml` を監視
- `gh run watch` で完了まで待機
- **失敗時**: `gh run view --log-failed` でログ取得 → 原因調査 → 修正 → 再プッシュ → 再度監視
- **成功時**: ユーザーに「デプロイ完了」と報告。対応 Issue があればクローズ

## 7. デプロイ

- **検証環境**: `staging` ブランチにマージ/プッシュで検証へ反映
- **本番環境**: ユーザーが「いいよ」と言ったら `main` にマージし本番へ反映
- 検証で確認 → OK で本番、という流れを守る。詳細は `docs/SPEC_DEPLOYMENT.md`
- **新規プロジェクト時**: `docs/DEPLOY_VERIFICATION_GUIDE.md` を参照。デプロイ方法・結果確認・やってはいけないことの指針あり
